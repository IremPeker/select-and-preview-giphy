import React from "react";

const Header = () => {
  //the function name(Navigation) has to be the same with the tags  <Navigation></Navigation> inside App.js
  return <h3>Choose a Giphy Image to preview on the right</h3>;
};

export default Header;
